# Issue 768 — language-policy audit (slice 2)

Generated by `go run ./tools/langscan` on 2026-09-22 against the
slice 2 branch (`chore/issue-768-migrate-source-and-docs`).

## Summary

| Total findings | Status |
| --- | --- |
| 0 | **Clean** — slice 2 closes the slice 1 audit. |

The slice 1 audit reported 3 findings; this slice migrates the two
substantive ones and adds the third to the documented-out-of-scope
list. The scanner now reports zero findings on a fresh run.

## Migration actions taken in slice 2

### 1. `.agents/skills/zod/references/error-i18n.md` — exempt by path

The file is a third-party agent skill reference (`agent-skills`
framework). It demonstrates how a Spanish translator would write
locale entries alongside the English source. Rewriting it would
defeat the document's purpose, so the slice adds `.agents/skills/`
to the scanner's path exclusions. The exclusion comment in the
scanner documents why this category is exempt.

### 2. `.agents/skills/` path exclusion added

Added to `tools/langscan/main.go` `excludedPathPrefixes` with the
rationale "third-party agent skills (installed by the agent-skills
framework)". The exclusion is documented in CONTRIBUTING.md and
in the scanner source.

### 3. `odd/tasks/` path exclusion added

The slice 1 plan and audit docs quote Spanish lines verbatim as
evidence. Added to the exclusions with the rationale "ODD workspace
docs may quote non-English artefacts verbatim as evidence".

### 4. `internal/service/env_bulk_test.go:L125` — exemption marker

The line `value := strings.Repeat("é", 4095) + "v"` carries a
Spanish-specific character but is intentional Unicode payload for
an env-var bulk test. Added an in-line `// l10n: technical-unicode-
fixture` marker above it. The scanner recognises both forms
(inline on the same line, or on its own line above the snippet) and
exempts the marked snippet from reporting.

### 5. Scanner empty-findings output

The CLI now prints `[]` (instead of `null`) when no findings are
reported, so shell callers can `jq .` the output safely.

## Excluded by design (final list)

The scanner does NOT report the following on purpose:

- `frontend/src/**` — user-visible UI strings, migrated to i18n
  catalogs in issue #767.
- `frontend/e2e/**` — Playwright specs mirror UI strings; migrated
  with the catalog in #767.
- `auditoria/**` — historical audit documents are immutable.
- `tools/langscan/**` — self-reference: the scanner code and test
  fixtures use Spanish on purpose.
- `odd/tasks/**` — ODD workspace docs may quote non-English artefacts
  verbatim as evidence.
- `.agents/skills/**` — third-party agent skills (installed by the
  agent-skills framework).
- `node_modules/`, `vendor/`, `frontend/dist/`, `dist/`, `build/`,
  `tmp/`, `.git/`, `.next/` — third-party or generated.

## Acceptance

- `go run ./tools/langscan` exits 0 and emits `[]`.
- All 10 scanner tests pass (8 from slice 1 + 2 new for inline /
  preceding-line exemption markers).
- `go test ./internal/service/...` continues to pass with the new
  marker comment.
