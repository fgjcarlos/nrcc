# Issue 768 — initial language-policy audit

Generated by `go run ./tools/langscan` on 2026-09-22 against
origin/main (`6c49691`).

## Summary

| Total findings | File |
| --- | --- |
| 2 | `.agents/skills/zod/references/error-i18n.md` |
| 1 | `internal/service/env_bulk_test.go` |

## Findings

### `.agents/skills/zod/references/error-i18n.md`

- L47: `invalidEmail: 'Por favor ingrese un correo electrónico válido',`
- L49: `tooYoung: (min: number) => \`Debes tener al menos ${min} años\`,`

**Classification**: illustrative. The file is an agent skill
reference documenting zod + i18n patterns; the Spanish strings are
example locale entries shown alongside the English translation.
Slice 2 will either rewrite the file in English with English
examples or add a per-line exemption marker.

### `internal/service/env_bulk_test.go`

- L125: `value := strings.Repeat("é", 4095) + "v"` (Unicode env-var fixture)

**Classification**: technical fixture. The `é` is intentional Unicode
payload for an env-var bulk test. Slice 2 will add a per-line
exemption marker (`// l10n: technical-unicode-fixture`) so the
scanner ignores the line while still flagging real Spanish comments.

## Excluded by design

The scanner does NOT report the following on purpose (see
`tools/langscan/main.go`):

- `frontend/src/**` — user-visible UI strings, migrated to i18n
  catalogs in issue #767.
- `frontend/e2e/**` — Playwright specs mirror UI strings; migrated
  with the catalog in #767.
- `auditoria/**` — historical audit documents are immutable.
- `tools/langscan/**` — self-reference: the scanner code and test
  fixtures use Spanish on purpose.
- `node_modules/`, `vendor/`, `frontend/dist/`, `dist/`, `build/`,
  `tmp/`, `.git/`, `.next/` — third-party or generated.

## Acceptance

The slice 1 scanner reduces the open-policy report from the manually
audited baseline (approximately 30 Spanish artefacts in non-frontend
files, mostly in `auditoria/`) to the 3 illustrative / technical
findings listed above. Slice 2 will land exemption patterns for those 3.
