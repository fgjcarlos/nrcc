// Re-export dashboard-specific types from shared
// These were originally in shared/types but are only used by dashboard
export type { RuntimeStatus, RuntimeInfo, ProcessMemory, SystemInfo } from '@/shared/types';
