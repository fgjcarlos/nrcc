# Server Configuration
PORT=3001
DATA_DIR=./data

# Edge mode (opt-in; default false). When true, NRCC prefers resource-safe
# defaults for constrained edge/Raspberry Pi/NAS deployments. See ADR 0002.
EDGE_MODE=false

# Authentication
JWT_SECRET=change-me-in-production

# Node-RED Process
NODE_RED_CMD=node-red
NODE_RED_PORT=1880

# Docker (optional)
DOCKER_SOCKET=/var/run/docker.sock
DOCKER_CONTAINER=nodered

# Environment Variable Encryption
ENCRYPTION_KEY=change-me-in-production

# CORS Configuration
CORS_ORIGINS=*

# Update Detection Configuration
NRCC_UPDATE_POLL_INTERVAL=4h
NRCC_UPDATE_TIMEOUT=10s
